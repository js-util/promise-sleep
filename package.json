{
	"name": "@js-util/promise-sleep",
	"version": "1.0.3",
	"description": "Returns a promise, which resolve after the specified sleep itme (ms)",

	"keywords": [
		"promise",
		"sleep"
	],

	"main": "promiseSleep.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},

	"author": "picocreator",
	"license": "MIT",
	"private": false,

	"repository": {
		"type": "git",
		"url": "https://github.com/js-util/promise-sleep.git"
	},
	
	"engines": {
		"node": ">=8.0.0"
	}
}
